---
title: "Template for Non-Targeted Analysis Standard Operating Procedure"
author: "autogenerated"
date: "`r Sys.Date()`"
output: word_document
params:
  dat: NA
---

**NOTICE**: This is an auto-generated template for standard operating procedures for non-targeted analysis, it is recommended to further refine and add text for the final standard operating procedure

## Background / Objective of Standard Operating Procedure

**An example of inline text, you could type something like:**

For your method, you selected `r dat["1A_1", "input"]` as your method category.

The following variables are available

```{r echo=FALSE}
ind <- grep("1[[:alpha:]]_", rownames(dat))
knitr::kable(dat[ind,c("description", "input")])
```

## Materials / Chemicals

Some sort of information could go here...


## Sample Information and Sampling

The following variables are available

```{r echo=FALSE}
ind <- grep("2[[:alpha:]]_", rownames(dat))
knitr::kable(dat[ind,c("description", "input")])
```


## Standards and Controls

The following variables are available

```{r echo=FALSE}
ind <- grep("3[[:alpha:]]_", rownames(dat))
knitr::kable(dat[ind,c("description", "input")])
```

## Data Aquisition

The following variables are available

```{r echo=FALSE}
ind <- grep("4[[:alpha:]]_", rownames(dat))
knitr::kable(dat[ind,c("description", "input")])
```


## Data Processing

The following variables are available

```{r echo=FALSE}
ind <- grep("5[[:alpha:]]_", rownames(dat))
knitr::kable(dat[ind,c("description", "input")])
```

## Quality Assurance / Quality Control

The following variables are available

```{r echo=FALSE}
ind <- grep("6[[:alpha:]]_", rownames(dat))
knitr::kable(dat[ind,c("description", "input")])
```

The following variables are available

## Results Reporting
